import { shortest } from "@antiwork/shortest";

shortest("Login to the App using magic link. Use this email: 'stay-ants@taoyeoeo.mailosaur.net'");
shortest("Verify that the user can access the /dashboard page");
